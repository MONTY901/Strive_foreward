class Solution {
    public static String compareNM(int n, int m) {
        if (n < m) {
            System.out.println("lesser");
        } 
        else if (n == m) {  // Fixed `else-if` and comparison
            System.out.println("equal");
        } 
        else {
            System.out.println("greater");
        }
        return ""; // Added return statement to match method signature
    }
}
